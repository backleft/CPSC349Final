import React from "react";
import Button from "./Button";
import PropTypes from "prop-types";

export default class ButtonConsole extends React.Component {
    static propTypes = {
        clickHandler: PropTypes.func,
    };
    handleclick
}